@page "/boundary-b04"
@using UnionValidationApp.Client.Models
@using UnionValidationApp.Client.Components.BoundaryTests

<h4>B04: NullableIntOrString (active int) — Server boundary</h4>
<B03_NullableBoundary @rendermode="InteractiveServer" Value="@value" />

@code {
    private NullableIntOrString value = (int?)42;
}
