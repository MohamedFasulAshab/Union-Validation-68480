@page "/boundary-b01"
@using UnionValidationApp.Client.Models
@using UnionValidationApp.Client.Components.BoundaryTests

<h4>B01: IntOrString (active int) — Server boundary</h4>
<B01_IntOrStringBoundary @rendermode="InteractiveServer" Value="@value" />

@code {
    private IntOrString value = 200606;
}
